# 审查意见归档

> 审查时间：2026-02-25
> 状态：已处理

***

## 一、关键问题

### 1.1 🔴 P0 - API 可用性风险

**问题**：TS 项目 README 明确警告 "V3.x does not support the new Bing Rewards interface"，但方案假设 API 仍有效。

**修正**：

* ✅ 添加"API 可用性验证"作为前置任务
* ✅ 保留 HTML 解析作为 fallback 方案
* ✅ 验证失败时调整方案

**处理结果**：2026-02-26 完成 API 验证，Dashboard API 可用。

### 1.2 🔴 P0 - 查询源重复建设

**问题**：方案计划新建 `BingSuggestionsSource` 和 `BingRelatedSource`，但：

* 已有 `src/search/bing_api_client.py` - 使用 `api.bing.com/osjson.aspx`
* 已有 `BingSuggestionsSource` 在 `src/search/query_sources/`

**修正**：

```diff
- 实现 `BingSuggestionsSource` - 获取搜索建议
- 实现 `BingRelatedSource` - 获取相关搜索
+ 扩展现有 BingAPIClient，添加 get_related_terms() 方法
+ 扩展现有 BingSuggestionsSource，而非新建
```

**处理结果**：已在开发计划中修正。

***

## 二、中等问题

### 2.1 🟡 P1 - 时间估算过于乐观

| 分支                            | 原预估   | 调整后   | 原因         |
| ----------------------------- | ----- | ----- | ---------- |
| `feature/dashboard-api`       | 3-5 天 | 3-4 天 | API 已验证可用  |
| `feature/app-api`             | 3-5 天 | 5-8 天 | OAuth 流程复杂 |
| `feature/benefits-collection` | 3-4 天 | 3-4 天 | 使用 API 简化  |

**处理结果**：已在开发计划中调整。

### 2.2 🟡 P1 - 测试缺口未纳入

`REWARDS_V2_ADAPTATION.md` 记录的 P0 测试缺口：

| 缺口                | 文件数 | 建议     |
| ----------------- | --- | ------ |
| `login/handlers/` | 9   | 并行补充测试 |
| `tasks/handlers/` | 3   | 并行补充测试 |
| `account/`        | 2   | 并行补充测试 |

**处理结果**：已纳入持续并行任务。

### 2.3 🟡 P1 - 依赖管理问题

`pyproject.toml` 缺少 `dependencies` 声明，建议在 `refactor/constants-consolidation` 分支中一并处理。

**处理结果**：constants-consolidation 已合并 (#11)，依赖问题待后续处理。

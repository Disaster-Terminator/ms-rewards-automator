# v1 AI 审查工具参考指南

> **归档说明**：此文档为 v1 版本，已归档。正常情况下请使用 v2 skill。
> 如果 CLI 工具或 GraphQL API 失败，可参考此文档作为降级方案。

> **重要区分**：Qodo v1/v2 是指工具版本，与项目架构 v1/v2 是不同概念。
>
> - Qodo v1 (PR Review)：使用 `/review` 命令 - 已归档
> - Qodo v2 (Code Review)：使用 `/agentic_review` 命令 - 当前使用

### 改进意见摘要

以下内容包含具体的改进建议：

| 来源 | 标题 | 存储位置 | 用途 |
|------|------|----------|------|
| Sourcery | "Prompt for AI Agents" | Review body | 完整审查的结构化摘要 |
| Qodo v2 | "Code Review" | Issue Comment + Review Thread | 改进意见摘要 |

**这些是需要处理的改进意见！**

---

## 三种来源完整对比

| 特性 | Sourcery | Qodo v2 | Copilot |
|------|----------|---------|---------|
| **PR 创建时** | 改动分析 + 完整审查 | Code 审查（自动执行 `/agentic_review`） | 一次审查 |
| **重审行为** | 全新完整审查 | 更新 Code Review | 无 |
| **行级评论** | ✅ 有 | ✅ 所有等级 | ✅ 有 |
| **行级评论解决** | `@sourcery-ai resolve` 或 API | ⚠️ **必须用 API** | GitHub API |
| **结构化摘要** | `Prompt for AI Agents` | `Code Review` | 无 |
| **解决标记** | `✅ Addressed` | `☑` | 无自动标记 |
| **斜杠命令** | `@sourcery-ai review` | `/agentic_review` | 无 |

---

## Sourcery

### 机器人 ID

- GitHub 用户名：`sourcery-ai bot`

### 完整流程

```
PR 创建
├── 改动分析（Reviewer's Guide）
└── 完整审查（Review）
    ├── 总览意见（high level feedback）
    └── Prompt for AI Agents（完整审查的结构化摘要）
        ├── Overall Comments
        └── Individual Comments（对应行级评论）

重审（@sourcery-ai review）
└── 全新的完整审查（新的 Review）
```

### 关键点

- **Prompt for AI Agents = 完整审查的结构化摘要**
- **Individual Comments 对应行级评论**
- **行级评论可通过 `@sourcery-ai resolve` 解决**
- **动态解决状态**：Sourcery 会根据新推送的 commit 自动检测并更新评论状态，标记为 `✅ Addressed in {commit_hash}`

### 评论结构

```
reviews.body 结构：
├── 总览意见（Overall Comments）- 无法单独解决
│   └── "In MSRewardsApp.__init__ the diagnosis_reporter..."
└── <details><summary>Prompt for AI Agents</summary>
    └── ## Individual Comments
        ├── ### Comment 1
        │   ├── <location> `file.py:42` </location>
        │   └── <issue_to_address>问题描述</issue_to_address>
        └── ### Comment 2
            └── ...
```

### 类型判断

| 标签 | 含义 | 处理方式 |
|------|------|----------|
| `bug_risk` | 潜在 Bug | 必须修复 |
| `security` | 安全问题 | 必须修复 |
| `suggestion` | 代码建议 | 自主决断 |
| `performance` | 性能建议 | 自主决断 |

### 解决状态检测

- **已解决**：body 含 `✅ Addressed in {commit}`
- **数据源**：`get_pull_request_comments` 返回的行级评论

### 交互命令

| 命令 | 用途 |
|------|------|
| `@sourcery-ai review` | 触发新审查 |
| `@sourcery-ai resolve` | 解决所有评论 |
| `@sourcery-ai dismiss` | 关闭所有审查 |

---

## Qodo

### Qodo v1 (PR Review) - 已归档

- 斜杠命令：`/review`
- 产物：Issue Comment (PR Review)

> **注意**：以下为 v1 特性说明，仅供参考。v2 特性见下文。

#### 机器人 ID

- GitHub 用户名：`qodo-code-review bot`

#### 完整流程

```
PR 创建
├── 改动分析（Review Summary by Qodo）
└── Code 审查（Code Review by Qodo）
    ├── 类型统计：🐞 Bugs (4) 📘 Rule violations (3) 📎 Requirement gaps (0)
    │
    ├── Action required（有行级评论）
    │   ├── 1. cli.py prints raw exception 📘 Rule violation ⛨ Security
    │   └── 2. 另一个问题... 🐞 Bug
    │
    ├── Remediation recommended / review recommend（无行级评论）
    │   ├── 1. 某个建议... ✓ Correctness
    │   └── 2. 另一个建议... ⚯ Reliability
    │
    └── Advisory comments / other（无行级评论）
        └── 1. 某个建议...

第一次 /review
└── PR Reviewer Guide 🔍（改进意见摘要）

后续 /review
├── 更新通知评论（Persistent review updated...）
└── PR Reviewer Guide 内容更新（同一 Issue Comment）
```

#### 关键点

- **所有意见都有状态显示（☑）**，不仅仅是行级评论
- **三种意见类型**：
  - Action required → 有行级评论
  - Remediation recommended（页面显示：review recommend）→ 无行级评论
  - Advisory comments（页面显示：other）→ 无行级评论
- **编号固定**：总览和行级评论的编号一一对应
- **行级评论无法通过 @ 解决**，必须用 GitHub API
- **PR Reviewer Guide 存储在 Issue Comments**
- **行级评论格式**：`编号. 问题标题 Emoji类型...`（如 `3. Ci依赖安装会失效 🐞 Bug ⛯ Reliability`）

#### 意见类型对照

| API/元素名称 | 页面显示名称 | 行级评论 |
|-------------|-------------|----------|
| Action required | Action required | ✅ 有 |
| Remediation recommended | review recommend | ❌ 无 |
| Advisory comments | other | ❌ 无 |

#### 评论类型

| 类型 | 内容 | 触发方式 | API返回 | 存储位置 |
|------|------|----------|---------|----------|
| **Code Review by Qodo** | 问题列表（Bug/Rule violation等） | PR创建时自动 | ❌ **空字符串** | Review Thread |
| **Review Summary by Qodo** | 代码变化摘要（非改进意见！） | PR创建时自动 | ✅ 有 | Issue Comment |
| **PR Reviewer Guide 🔍** | 审查指南（改进意见摘要） | 首次 `/review` | ✅ 有 | Issue Comment |
| **Persistent review updated** | 更新通知 | 后续 `/review` | ✅ 有 | Issue Comment |

**重要**：Code Review by Qodo 的 body 通过 GraphQL 和 REST API 都返回**空字符串**，无法获取完整内容。只能通过 Review Thread API 获取 Action required 的行级评论。

#### PR Reviewer Guide 内容结构

```
PR Reviewer Guide 🔍
(Review updated until commit <hash>)

⏱️ Estimated effort to review: X
🧪 PR contains tests
🔒 No security concerns identified
⚡ Recommended focus areas for review

[问题类型 1]
问题描述...

[问题类型 2]
问题描述...
```

#### 特征标记

| 标记 | 类型 | 处理方式 |
|------|------|----------|
| 🐞 Bug | Bug | 必须修复 |
| 📘 Rule violation | 规则违反 | 必须修复 |
| ⛨ Security | 安全问题 | 必须修复 |
| ⚯ Reliability | 可靠性问题 | 必须修复 |
| Correctness | 正确性问题 | 自主决断 |

#### 解决状态检测

**Code Review by Qodo**：

- **已解决**：评论行开头有 `☑`（一个勾）
- **重要**：`✓` 符号是类型前缀（如 `✓ Correctness`），不是已解决标志！
- **重要**：行级评论**无法通过 @ 解决**，必须调用 GitHub API
- **重要**：所有意见类型都有状态显示，不仅仅是行级评论

**PR Reviewer Guide 🔍**：

- 不包含具体问题，只是审查指南
- 直接报告给用户

#### 交互命令

| 命令 | 用途 | 版本 |
|------|------|------|
| `/review` | PR 审查（v1） | v1 |
| `/describe` | 生成 PR 描述 | v1 |
| `/improve` | 代码建议 | v1 |
| `/checks ci_job` | CI 反馈 | v1 |

---

### Qodo v2 (Code Review) - 当前使用

#### 机器人 ID

- GitHub 用户名：`qodo-code-review bot`

#### 完整流程

```
PR 创建
└── 自动执行 /agentic_review
    └── Code Review (Issue Comment)
        ├── 问题列表（Bug/Rule violation等）
        └── 逐行评论（通过 Review Thread）

后续 /agentic_review
├── 更新说明（Persistent review updated to latest commit <hash>）
└── 新增逐行评论（通过 Review Thread）
```

#### 关键点

- **自动执行**：项目创建时自动触发
- **唯一性**：每个 PR 只有一个 Code Review
- **更新方式**：再次执行 `/agentic_review` 更新
  - 在原 Issue Comment 添加更新说明：`Persistent review updated to latest commit <hash>`
  - 新增逐行评论（Review Thread）
- **逐行评论**：通过 Review Thread 获取
- **状态标记**：`☑` 表示已解决
- **所有等级都会成为逐行评论**：项目设定 `inline_comments_severity_threshold: 1`

#### 评论类型

| 类型 | 内容 | 触发方式 | 存储位置 |
|------|------|----------|----------|
| **Code Review** | 问题列表（Bug/Rule violation等） | PR创建时自动 / `/agentic_review` | Issue Comment |
| **更新说明** | `Persistent review updated to latest commit <hash>` | 后续 `/agentic_review` | Issue Comment |
| **逐行评论** | 具体代码位置的问题 | 通过 `/agentic_review` 或自动 | Review Thread |

#### 特征标记

| 标记 | 类型 | 处理方式 |
|------|------|----------|
| 🐞 Bug | Bug | 必须修复 |
| 📘 Rule violation | 规则违反 | 必须修复 |
| ⛨ Security | 安全问题 | 必须修复 |
| ⚯ Reliability | 可靠性问题 | 必须修复 |
| Correctness | 正确性问题 | 自主决断 |

#### 解决状态检测

- **已解决**：评论行开头有 `☑`（一个勾）
- **注意**：`✓` 符号是类型前缀（如 `✓ Correctness`），不是已解决标志！
- **行级评论无法通过 @ 解决**，必须用 GitHub API

#### 交互命令

| 命令 | 用途 |
|------|------|
| `/agentic_review` | 触发 Code Review（PR创建时自动执行，后续更新） |
| `/agentic_describe` | 生成 PR 描述 |

---

## Copilot

### 机器人 ID

- GitHub 用户名：`Copilot AI`

### 完整流程

```
PR 创建
└── 一次审查（无重审机制）
```

### 评论结构

```
Copilot AI left a comment
├── Pull request overview（总览意见）
│   ├── 审查统计：reviewed X out of Y changed files
│   ├── 生成的评论数：generated N comments
│   └── 低置信度评论：Comments suppressed due to low confidence (M)
│
└── 行级评论
    ├── 文件路径
    ├── 状态标记：Outdated / Resolved 等
    ├── 位置：lines X to Y
    ├── 代码片段
    ├── 评论内容
    └── 免责声明：Copilot uses AI. Check for mistakes.
```

### 关键点

- **仅在 PR 创建时审查一次**
- **无后续重审机制**
- **有置信度概念**：低置信度评论会被抑制
- **有状态标记**：Outdated / Resolved 等

### 类型判断

| 类型 | 处理方式 |
|------|----------|
| 代码建议块（```suggestion） | 自主决断 |
| 安全警告 | 必须修复 |

### 解决状态检测

- **状态标记**：Outdated / Resolved 等
- **解决方式**：GitHub API 或手动点击"解决对话"

### 交互命令

- 无命令交互，自动审查

---

## 处理建议汇总

| 评论类型 | Agent 行为 |
|----------|------------|
| `bug_risk`, `Bug`, `Security`, `Rule violation`, `Reliability` | 报告给用户，等待修复指令 |
| `suggestion`, `performance`, `Correctness` | 报告给用户，自主决断是否采纳 |
| Code Review (Qodo v2) | 直接报告给用户（改进意见） |
| Reviewer's Guide (Sourcery) | 仅作参考（代码变化摘要，非改进意见） |
| Review Summary by Qodo | 仅作参考（代码变化摘要，非改进意见） |

## 合并提醒

- **Agent 不自动合并 PR**，需通知用户确认
- Sourcery 可用 `@sourcery-ai resolve` 批量解决
- **Qodo 行级评论必须调用 GitHub API 解决**
- Copilot 需调用 GitHub API 解决

---

## 解决依据类型

| 依据 | 说明 | 是否需要回复 |
|------|------|--------------|
| code_fixed | 代码已修复 | 否 |
| adopted | 已采纳建议 | 否 |
| rejected | 拒绝建议 | 是 |
| false_positive | 误报 | 是 |
| outdated | 过时 | 否 |

---

## 回复说明规则

| 情况 | 是否回复 | 回复内容 |
|------|----------|----------|
| 代码已修复 | 否 | - |
| 已采纳建议 | 否 | - |
| 拒绝建议 | 是 | 说明拒绝原因 |
| 误报 | 是 | 说明为何是误报 |
| 过时 | 否 | - |

---

## 严禁事项

1. **严禁一次性解决所有评论**：每个评论必须单独处理
2. **严禁无依据标记解决**：必须先确认问题已解决
3. **严禁批量操作**：必须逐个评论处理
4. **严禁跳过说明评论**：rejected/false_positive 必须回复
